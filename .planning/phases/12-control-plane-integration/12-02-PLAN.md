---
phase: 12-control-plane-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
autonomous: true

must_haves:
  truths:
    - "Drizzle schema adds containerId (text, nullable) and containerName (text, nullable) to subscriptions"
    - "Old VPS columns (hetznerServerId, hetznerSshKeyId, vpsIpAddress, vpsHostname, sshPrivateKey) are kept nullable for migration safety"
    - "provisioningStatus enum simplified: pending, creating, starting, ready, failed"
    - "Update tracking fields renamed: currentVersion→currentImage, targetVersion→targetImage, previousVersion→previousImage"
    - "claudeTokens table kept but no longer required for provisioning"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Updated Drizzle schema with Docker container fields"
      contains: "containerId"
---

<objective>
Update the database schema to support Docker containers instead of Hetzner VPS. This is a non-destructive migration: new columns are added, old columns are kept nullable.

Purpose: The database is the source of truth for user subscriptions and their infrastructure state. The schema must reflect the Docker container model while maintaining backward compatibility during the transition period.

Output: Updated src/lib/db/schema.ts with new container columns and updated enums.
</objective>

<execution_context>
@/home/rachel/.claude/get-shit-done/workflows/execute-plan.md
@/home/rachel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-control-plane-integration/12-RESEARCH.md
@src/lib/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Drizzle schema for Docker containers</name>
  <files>/home/rachel/rachel-cloud/src/lib/db/schema.ts</files>
  <action>
    Read the current schema.ts first to understand the exact structure.

    Make these changes to the subscriptions table:

    1. **Add new columns:**
       - `containerId` — text, nullable (Docker container ID)
       - `containerName` — text, nullable (e.g., "rachel-user-abc123")

    2. **Keep old VPS columns as nullable** (don't delete — migration safety):
       - `hetznerServerId`, `hetznerSshKeyId`, `vpsIpAddress`, `vpsHostname`, `sshPrivateKey`
       - Make sure they are all nullable (they should already be)

    3. **Add image tracking columns** (rename concept from version to image):
       - `currentImage` — text, nullable (e.g., "rachel8:latest" or "rachel8:abc123")
       - `targetImage` — text, nullable
       - `previousImage` — text, nullable
       - Keep old `currentVersion`/`targetVersion`/`previousVersion` columns as deprecated aliases

    4. **Do NOT change the provisioningStatus enum** if it would break existing data — instead, the application code can just use a subset of values. If the enum is defined as a text column, no change needed.

    5. **Ensure claudeTokens foreign key is optional** — the table stays but provisioning should not require it.

    **Important:** Read the file first to see the exact Drizzle ORM syntax used (SQLite vs Postgres, etc.), then make minimal targeted edits.
  </action>
  <verify>
    ```bash
    grep -q "containerId" /home/rachel/rachel-cloud/src/lib/db/schema.ts && \
    grep -q "containerName" /home/rachel/rachel-cloud/src/lib/db/schema.ts && \
    echo "PASS" || echo "FAIL"
    ```
  </verify>
  <done>Schema updated with containerId, containerName, currentImage, targetImage, previousImage columns. Old VPS columns kept nullable for migration safety. claudeTokens not required for provisioning.</done>
</task>

</tasks>

<verification>
1. containerId and containerName columns added to subscriptions table
2. Old VPS columns still present but nullable
3. Image tracking columns added
4. Schema file passes TypeScript type checking
5. Drizzle migration can be generated successfully
</verification>

<success_criteria>
- `bun run drizzle-kit generate` succeeds (if drizzle-kit is configured)
- New columns are nullable (don't break existing data)
- Both old VPS and new Docker columns coexist in the schema
</success_criteria>

<output>
After completion, create `.planning/phases/12-control-plane-integration/12-02-SUMMARY.md`
</output>
