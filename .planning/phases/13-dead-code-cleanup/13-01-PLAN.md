# Plan 13-01: Delete Dead Provisioning, Monitoring & Update Files

## Goal
Remove all Hetzner VPS, SSH-based monitoring, and SSH-based update code that was replaced by the Docker orchestrator in v2.0.

## Files to Delete (14 files)

### Provisioning (9 files)
- `src/lib/provisioning/hetzner-client.ts`
- `src/lib/provisioning/provision-vps.ts`
- `src/lib/provisioning/deprovision-vps.ts`
- `src/lib/provisioning/cloud-init-builder.ts`
- `src/lib/provisioning/ssh-injector.ts`
- `src/lib/provisioning/ssh-exec.ts`
- `src/lib/provisioning/ssh-keys.ts`
- `src/lib/provisioning/vps-status.ts`
- `src/lib/provisioning/types.ts`

### Monitoring (3 files)
- `src/lib/monitoring/health-checker.ts`
- `src/lib/monitoring/health-notifications.ts`
- `src/lib/monitoring/circuit-breaker.ts`

### Updates (1 file)
- `src/lib/updates/update-engine.ts`

### API Routes (1 endpoint)
- `src/routes/api/provision/callback/[userId]/+server.ts`

### Ops CLI (1 file)
- `ops.ts` (root-level SSH-based ops tool — uses ssh2 directly)

## Live Files That Need Fixing

### `src/hooks.server.ts`
- Remove `import { startHealthMonitor } from '$lib/monitoring/health-checker'`
- Remove the `startHealthMonitor()` call block (lines 8-11)
- Keep everything else (auth handler, session, admin guard)

## Dependencies to Remove

### package.json
- Remove `ssh2` from dependencies
- Remove `@types/ssh2` from devDependencies
- Run `bun install` to update lockfile

## Remaining Files (NOT deleted)

### `src/lib/provisioning/` — nothing remains, delete the directory
### `src/lib/monitoring/` — nothing remains, delete the directory
### `src/lib/updates/rollout-orchestrator.ts` — KEEP (already rewritten for Docker in Phase 12)
### `src/lib/onboarding/telegram-validator.ts` — KEEP (still validates bot tokens)

## Verification

After deletion:
1. `grep -r "provisioning/" src/ --include="*.ts" --include="*.svelte"` → should only find `$lib/onboarding/`-related or non-import references
2. `grep -r "health-checker\|health-notifications\|circuit-breaker" src/` → zero results
3. `grep -r "update-engine" src/` → zero results
4. `grep -r "ssh2\|ssh-exec\|ssh-keys\|ssh-injector" src/` → zero results
5. `bun run check` or build should pass

## Order of Operations
1. Delete all 16 files
2. Delete empty directories (`src/lib/provisioning/`, `src/lib/monitoring/`)
3. Fix `hooks.server.ts`
4. Remove ssh2 deps from package.json
5. Run `bun install`
6. Verify build
