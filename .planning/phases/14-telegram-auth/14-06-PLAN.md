# Plan 14-06: Remove Better Auth + Claude OAuth; Update Auth Guards

## Goal
Delete all Better Auth code, Claude OAuth code, Google OAuth config, and update route guards to use Telegram session.

## Files to Delete

### Auth Library (6 files)
- `src/lib/auth/config.ts` — Better Auth + Polar plugin config (webhook handlers move to standalone endpoint)
- `src/lib/auth/client.ts` — Frontend Better Auth client
- `src/lib/auth/claude-oauth.ts` — Claude OAuth 2.0 + PKCE flow
- `src/lib/auth/claude-token-manager.ts` — Claude token refresh logic
- `src/lib/auth/rate-limit.ts` — Rate limiting middleware (can recreate if needed)

### Web Pages
- `src/routes/login/` — login page (replaced by Telegram Login Widget page)
- `src/routes/signup/` — signup page (no longer needed)
- `src/routes/api/auth/` — Better Auth API routes (replaced by /api/auth/telegram)

### Onboarding (modify, not delete)
- `src/routes/(app)/onboarding/` — simplify: remove BotFather instructions, just show "subscribing..." flow

## Files to Keep (rewritten in earlier plans)
- `src/lib/auth/session.ts` — rewritten in 14-02 for signed cookies
- `src/lib/auth/telegram-login.ts` — created in 14-02

## Files to Modify

### `src/hooks.server.ts`
Already rewritten in Plan 14-02. Remove:
- Better Auth handler (`auth.handler()`)
- Better Auth session (`getSession()`)
- Admin guard using email

Replace with:
- Signed cookie session verification
- Admin guard using `ADMIN_TELEGRAM_ID`

### `src/app.d.ts`
Update `App.Locals` type:
```typescript
declare global {
  namespace App {
    interface Locals {
      session: {
        telegramId: number;
        firstName: string;
        username?: string;
        photoUrl?: string;
      } | null;
    }
  }
}
```

### Dashboard Pages
Update all `(app)` route pages that reference `data.user` or `data.session`:
- `+layout.server.ts` — return session from locals
- `+layout.svelte` — show Telegram user info instead of email
- Dashboard pages — update user display (name, avatar from Telegram)

### Admin Pages
- `src/lib/admin/guard.ts` — replace `isAdmin(email)` with `isAdmin(telegramId)`
- Admin layout — show Telegram admin info

## Dependencies to Remove from package.json
- `better-auth`
- `@better-auth/cli` (if present)
- `@polar-sh/better-auth`

## Env Vars to Remove
- `BETTER_AUTH_SECRET`
- `GOOGLE_CLIENT_ID`
- `GOOGLE_CLIENT_SECRET`

## Env Vars to Add (if not already)
- `TELEGRAM_BOT_TOKEN` — shared bot token
- `ADMIN_TELEGRAM_ID` — admin's Telegram user ID

## Order of Operations
1. Delete Better Auth files (config, client, claude-oauth, token-manager, rate-limit)
2. Delete login/signup pages
3. Delete /api/auth routes
4. Remove Better Auth + Polar plugin dependencies
5. Update hooks.server.ts (already done in 14-02, just verify)
6. Update app.d.ts
7. Update dashboard layout + pages for Telegram session
8. Update admin guard
9. Simplify onboarding page
10. Remove unused env vars from .env
11. Verify build
